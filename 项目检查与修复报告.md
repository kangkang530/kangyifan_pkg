# 汽车总布置知识库 — 代码可行性检查与修复报告

## 项目概况

- **类型**：基于 MkDocs Material 的静态文档站点（Python 项目）
- **本机 Python**：3.12.7（`D:\uncle\ana anzhuangdizhi\python.exe`）
- **本次变动**：从其他电脑拷贝覆盖，新增了 `.venv` 虚拟环境与客户端登录功能

## 发现的问题与修复

### 问题 1（关键）：`.venv` 虚拟环境损坏，无法运行

- **现象**：`.venv\Scripts\python.exe` 报错 `Cannot find home in 'd:\uncle\personal_web\.venv\pyvenv.cfg'`，退出码 107。
- **根因**：原电脑的 `.venv` 被亿赛通（E-SafeNet）DLP 加密软件加密，拷贝到本机后 `pyvenv.cfg` 变为乱码二进制；且虚拟环境本就不可跨机器/跨 Python 版本直接拷贝使用。
- **修复**：删除损坏的 `.venv`，用本机 Python 3.12.7 重新创建虚拟环境，并安装 `requirements.txt` 全部依赖。
- **验证**：`python.exe --version` 正常；`check_env.py` 报告"环境满足 requirements.txt 的全部要求"。

### 问题 2：`requirements.txt` 编码导致 pip 安装失败

- **现象**：`pip install -r requirements.txt` 报 `UnicodeDecodeError: 'gbk' codec can't decode byte 0x96`。
- **根因**：`requirements.txt` 含中文注释，以 UTF-8 无 BOM 保存。原电脑系统区域为 UTF-8 可正常解析；本机系统区域为 GBK，pip 回退到 GBK 解码失败。
- **修复**：将 `requirements.txt` 转存为 UTF-8 with BOM（`utf-8-sig`），使 pip 在任意系统区域下均能正确识别编码。
- **验证**：依赖安装成功（mkdocs 1.6.1、mkdocs-material 9.7.7 等）。

### 问题 3：`check_env.py` 不兼容 BOM 文件

- **现象**：修复问题 2 后，`check_env.py` 将 BOM 字符 `\ufeff` 当成包名，误报"未安装的包: ﻿# MkDocs Material 知识库依赖"。
- **根因**：脚本以 `encoding="utf-8"` 读取文件，不会自动剥离首部 BOM。
- **修复**：将 `check_env.py` 中读取 `requirements.txt` 的编码改为 `utf-8-sig`（自动剥离 BOM，且兼容无 BOM 文件）。
- **验证**：环境检查脚本输出正常，4 个依赖全部满足。

### 问题 4：`.gitignore` 遗漏 `.venv` 忽略规则

- **现象**：`.gitignore` 仅忽略 `venv/`，但项目实际使用 `.venv`（`check_env.py`、`启动本地预览.bat`、README 均用 `.venv`），导致虚拟环境可能被误提交。
- **修复**：在 `.gitignore` 的 Python 段补充 `.venv/`。

## 登录功能检查（无问题）

新增的客户端登录功能完整且自洽：

| 文件 | 状态 |
|------|------|
| `docs/javascripts/auth.js` | 首页免登录、其他页需登录、右上角登录/退出按钮、弹窗回车提交，逻辑完整 |
| `docs/stylesheets/auth.css` | 按钮与弹窗样式完整，含移动端适配 |
| `docs/javascripts/users.json` | 含 1 个用户（kangyifan），SHA-256 加盐哈希 |
| `manage_users.py` | 语法正确，add/remove/list/passwd 命令正常，哈希算法与 `auth.js` 一致 |
| `mkdocs.yml` | 正确引用 `auth.css` 与 `auth.js` |

- 前后端哈希算法一致：均为 `SHA-256(salt + password)`。
- `mkdocs serve` 下 `users.json` 以 `application/json` 正确提供（HTTP 200）。

## 运行验证

- `mkdocs build --clean`：构建成功（11.20s），仅有 `maintain/update.md` 中代码块内的**示例占位链接**告警（`../xxx/yyy.md`），属正常，非真实坏链。
- `mkdocs serve`：服务正常启动，首页 HTTP 200（46824 字节），`users.json` HTTP 200（213 字节）。

## 修改文件清单

| 文件 | 改动 |
|------|------|
| `.venv/` | 删除损坏环境，用本机 Python 3.12.7 重建并安装依赖 |
| `requirements.txt` | 转为 UTF-8 with BOM 编码 |
| `check_env.py` | 读取编码改为 `utf-8-sig`，兼容 BOM |
| `.gitignore` | 补充 `.venv/` 忽略规则 |

## 结论

项目现已可在本机正常运行。日常使用双击 `启动本地预览.bat` 即可启动本地预览（脚本会自动识别 `.venv` 并启动 mkdocs serve）。
